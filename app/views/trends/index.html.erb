<table>
	<tr>
		<th>Trends</th>
	</tr>

<% @trends.each do |trend| %>
	<tr>
		<b><td><%= trend.name %></td></b>
		<td><%= trend.created_at %></td>
	</tr>
	<% trend.tweets.each do |tweet| %>
	<tr>
		<td><%= tweet.text %></td>
		<td><%= tweet.screen_name %></td>
	</tr>
	<% end %>
<% end %>
</table>
	