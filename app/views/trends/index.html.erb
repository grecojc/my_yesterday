<table>
	<tr>
		<th>Trends</th>
	</tr>

<% @trends.each do |trend| %>
	<tr>
		<td><%= trend.name %></td>
		<td><%= trend.created_at %></td>
	</tr>
	<% trend.tweets.each do |tweet| %>
	<tr>
		<td><%= tweet.name %></td>
		<td><%= tweet.created_at %></td>
	</tr>
<% end %>
</table>